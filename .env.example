# GitHub personal access token (optional but strongly recommended).
# Contents: repo scope is enough for public repositories.
# Leave empty only for tiny unauthenticated experiments.
GITHUB_TOKEN=

# Groq API key for explanations (optional for ML-only: python run.py --no-llm ...).
GROQ_API_KEY=
